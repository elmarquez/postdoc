// pixels
const CONTENT_PANEL_WIDTH = 240;

// the screen edge where the panel is attached
const EDGE_PANEL_POSITION = {
  BOTTOM: 'bottom',
  LEFT: 'left',
  RIGHT: 'right',
  TOP: 'top'
};

// the width or depth of the tab panel, depending on which screen edge the
// panel is attached to
const TAB_PANEL_WIDTH = 30; // pixels

export { CONTENT_PANEL_WIDTH, EDGE_PANEL_POSITION, TAB_PANEL_WIDTH };
